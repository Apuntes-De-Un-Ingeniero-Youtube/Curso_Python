# Creando un array
array = [1,2,5,78,0,45]
print(array)

# Añadiendo contenido al array
array.append(77)
print(array)

# Removiendo contenido del array
array.remove(1)
print(array)

# Revirtiendo el array
array.reverse()
print(array)

# Ordenando el array
array.sort()
print(array)

# Conociendo la longitud dek vector (arreglo)
print(len(array))